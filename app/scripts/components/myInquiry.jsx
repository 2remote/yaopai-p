var React = require('react');
var Panel = require('react-bootstrap').Panel;

var MyInquiry = React.createClass({

  render: function() {

    return (
      <Panel>
      </Panel>
    );
  }
});

module.exports = MyInquiry;
