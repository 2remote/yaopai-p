const log = console.log

const info = console.info

const warn = console.warn

const error = console.error

export default {
  log,
  info,
  warn,
  error,
}

export { log }
export { info }
export { warn }
export { error }
